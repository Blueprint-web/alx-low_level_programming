#include "main.h"

int _sqrt_recursion(int n)
{
	int i = 0;
	int end = n, mid;
	n /= 2;
	while (i <= end)
	{
		mid = (i + n) / 2;
		if (mid * mid == n)
		{
			ans = mid;
			break;
		}
		if (mid * mid < n)
		{
			ans = i
	return(i);
	if (i * i > n)
		return(-1);
}
