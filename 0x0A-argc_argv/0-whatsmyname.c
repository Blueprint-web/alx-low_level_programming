#include <stdio.h>
/**
 * main - the arg main
 * @argc: the number of argument
 * @argv: the string array
 * Return: 0 success
 */
int main(int argc, char *argv[])
{
	int i = 1;

	if (argc == i)
		printf("%s\n", argv[0]);
	return (0);
}
