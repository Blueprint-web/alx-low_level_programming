#include <stdio.h>
/**
 * main -  writes its name to stdout
 * @argc: arguement count
 * @argv: aguement vector
 * Return: 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
